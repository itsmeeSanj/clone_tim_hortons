const theme = {
  token: {
    // Seed Token
    colorPrimary: "rgb(200, 16, 46)",
    borderRadius: 2,

    // Alias Token
    colorBgContainer: "#f6ffed",
  },
};

export default theme;
